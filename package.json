{
  "name": "jlpt-n3-grammar-lab",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name jlpt-n3-grammar-lab --branch main"
  }
}
